# Local, per-machine plugin enablement — never commit (gitignored by convention)
.claude/settings.local.json

# Python
__pycache__/
*.pyc

# Design/planning docs — kept locally, not shipped with the plugin
docs/
